pipeline {
   agent any
   stages {
      stage('Checkout Code') {
         steps {
            // Git checkout
         }
      }
      stage('Install Dependencies') {
         steps {
            // npm install
         }
      }
      stage('Build Application') {
         steps {
            // npm run build
         }
      }
      stage('Docker Build') {
         steps {
            // docker build
         }
      }
      stage('Deploy to VM2') {
         steps {
            // ssh
            // docker stop
            // docker rm
            // docker run
         }
      }
   }
}
